"use client";

import { useShoppingCart } from "@/context/ShoppingCartContext";
import React, { useEffect, useLayoutEffect } from "react";

const CheckoutReturnLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default CheckoutReturnLayout;
